using System;
using System.Collections;
// using System.Collections.Generic;
// using Sirenix.OdinInspector;
using UnityEngine;

// https://www.youtube.com/watch?v=F_LtpgpTHA8

public class SquashAndStretch : MonoBehaviour
{
    [Header("Notes")]
    [SerializeField, Multiline(lines:2)] private string notes;

    // [Header("Squash and Stretch Core")]
    // [SerializeField, Tooltip("Defaults to current game object is not set."),ChildGameObjectsOnly] private Transform transformToAffect;
    
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
